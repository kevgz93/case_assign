var userModel = require('../data/average.model.js');
var mongoose = require('mongoose');

var q = require('q');

var db = mongoose.model('average');
var average = {};


// when a time off start, we changes the values from working days.
users.addAverage = function(req, res)
{
    let average;
}